<?xml version='1.0' encoding='utf-8'?>
<widget id="safety.call.japan" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:android="http://schemas.android.com/apk/res/android">
    <name>Safety Call Japan</name>
    <description>نظام حظر المكالمات الدولية</description>
    
    <content src="index.html" />
    
    <!-- إضافات الصلاحيات الأساسية -->
    <plugin name="cordova-plugin-android-permissions" spec="^1.1.5" />
    <plugin name="cordova-plugin-device" spec="^2.1.0" />
    <plugin name="cordova-plugin-androidx-adapter" spec="^1.1.3" />

    <platform name="android">
        <!-- التصريح عن الصلاحيات في ملف المانيفست -->
        <config-file target="AndroidManifest.xml" parent="/*">
            <uses-permission android:name="android.permission.READ_PHONE_STATE" />
            <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
            <uses-permission android:name="android.permission.READ_CALL_LOG" />
            <uses-permission android:name="android.permission.MODIFY_PHONE_STATE" />
        </config-file>

        <!-- ربط خدمة فحص المكالمات (Call Screening Service) -->
        <config-file target="AndroidManifest.xml" parent="/manifest/application">
            <service android:name="com.cordova.plugins.CallScreeningImplementation"
                     android:permission="android.permission.BIND_SCREENING_SERVICE"
                     android:exported="true">
                <intent-filter>
                    <action android:name="android.telecom.CallScreeningService" />
                </intent-filter>
            </service>
        </config-file>
    </platform>

    <preference name="android-minSdkVersion" value="29" />
    <preference name="android-targetSdkVersion" value="34" />
</widget>
